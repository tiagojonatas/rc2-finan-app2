<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Visao Geral - RC2 Finance</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            'sans': ['Inter', 'system-ui', 'sans-serif'],
          },
          colors: {
            'custom-bg': '#f1f5f9',
            'brand-purple': '#8A05BE',
            'brand-purple-light': '#9D4EDD',
            'brand-purple-dark': '#6A047E',
            'brand-purple-50': '#F8F0FF',
            'brand-purple-100': '#E9D5FF',
            'brand-purple-200': '#D4BBFF',
          }
        }
      }
    }
  </script>
  <style>
    body {
      background:
        radial-gradient(circle at 10% 8%, rgba(138, 5, 190, 0.28), transparent 34%),
        radial-gradient(circle at 90% 5%, rgba(157, 78, 221, 0.26), transparent 30%),
        radial-gradient(circle at 50% 100%, rgba(188, 132, 233, 0.22), transparent 38%),
        linear-gradient(180deg, #f7f2ff 0%, #f2f5ff 48%, #eef3ff 100%);
      font-family: 'Inter', system-ui, sans-serif;
      min-height: 100vh;
      background-attachment: fixed;
    }

    .glass-card {
      background: linear-gradient(140deg, rgba(255, 255, 255, 0.72), rgba(255, 255, 255, 0.45));
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      border: 1px solid rgba(206, 167, 239, 0.65);
    }

    .soft-shadow {
      box-shadow:
        0 18px 45px rgba(106, 4, 126, 0.12),
        0 3px 10px rgba(15, 23, 42, 0.06);
    }

    .header-glass-btn {
      background: rgba(255, 255, 255, 0.66);
      border: 1px solid rgba(212, 187, 255, 0.8);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
    }

    .toast-enter {
      opacity: 0;
      transform: translateY(8px) scale(0.98);
    }

    .toast-show {
      opacity: 1;
      transform: translateY(0) scale(1);
    }

    .tx-highlight {
      animation: txPulse 1.1s ease-out;
    }

    @keyframes txPulse {
      0% { background-color: rgba(138, 5, 190, 0.2); }
      100% { background-color: transparent; }
    }
  </style>
</head>
<body class="font-sans text-gray-900">
  <div class="container mx-auto px-4 sm:px-6 py-6 sm:py-8 max-w-5xl relative pb-28">
    <div class="flex items-center justify-between gap-3 mb-5 sm:mb-6">
      <div class="flex items-center gap-3">
        <div class="w-9 h-9 sm:w-10 sm:h-10 rounded-2xl bg-gradient-to-br from-brand-purple to-brand-purple-dark text-white flex items-center justify-center font-bold soft-shadow">
          RC
        </div>
        <p class="text-xl sm:text-2xl font-extrabold tracking-tight text-brand-purple">RC2 Finance</p>
      </div>
      <div class="flex items-center gap-2 sm:gap-3">
        <a href="/credit-cards" class="header-glass-btn min-h-[44px] hover:bg-brand-purple-100 text-brand-purple font-medium py-2 px-3 sm:px-4 rounded-2xl transition-all duration-200 hover:scale-105 inline-flex items-center justify-center">
          Cartoes
        </a>
        <form action="/logout" method="POST">
          <button type="submit" class="header-glass-btn min-h-[44px] hover:bg-brand-purple-50 text-gray-700 hover:text-brand-purple font-medium py-2 px-3 sm:px-4 rounded-2xl transition-all duration-200 hover:scale-105">
            Sair
          </button>
        </form>
      </div>
    </div>

    <div class="flex flex-col sm:flex-row sm:justify-between sm:items-start gap-4 mb-7 sm:mb-8">
      <div>
        <h1 class="text-2xl sm:text-3xl font-bold text-gray-900">Visao Geral</h1>
        <p id="currentMonth" class="text-base sm:text-lg text-brand-purple mt-1.5 sm:mt-2 font-medium"></p>
      </div>
      <div class="w-full sm:w-auto flex flex-col sm:flex-row sm:flex-wrap items-stretch sm:items-center sm:justify-end gap-2 sm:gap-3">
        <form id="monthFilterForm" action="/dashboard" method="GET" class="header-glass-btn rounded-2xl px-3 py-2 min-h-[44px] flex items-center gap-2">
          <label for="monthFilter" class="text-xs font-semibold text-gray-600 uppercase tracking-wide">Periodo</label>
          <select id="monthFilter" name="month" class="bg-transparent text-sm font-semibold text-brand-purple focus:outline-none w-full sm:w-auto">
            <% const safeMonthOptions = Array.isArray(monthOptions) ? monthOptions : []; %>
            <% safeMonthOptions.forEach((monthKey) => { %>
              <% const [year, month] = monthKey.split('-').map(Number); %>
              <% const monthLabel = new Date(year, month - 1, 1).toLocaleDateString('pt-BR', { month: 'long', year: 'numeric' }); %>
              <option value="<%= monthKey %>" <%= monthKey === selectedMonth ? 'selected' : '' %>><%= monthLabel %></option>
            <% }); %>
          </select>
        </form>
        <a href="/reports" class="header-glass-btn min-h-[44px] hover:bg-brand-purple-100 text-brand-purple font-medium py-2 px-4 rounded-2xl transition-all duration-200 hover:scale-105 inline-flex items-center justify-center">
          Relatorios
        </a>
        <a href="/categories" class="header-glass-btn min-h-[44px] hover:bg-brand-purple-100 text-brand-purple font-medium py-2 px-4 rounded-2xl transition-all duration-200 hover:scale-105 inline-flex items-center justify-center">
          Categorias
        </a>
        <a href="/fixed-expenses" class="header-glass-btn min-h-[44px] hover:bg-brand-purple-100 text-brand-purple font-medium py-2 px-4 rounded-2xl transition-all duration-200 hover:scale-105 inline-flex items-center justify-center">
          Fixas
        </a>
      </div>
    </div>

    <% if (typeof dashboardToast !== 'undefined' && dashboardToast) { %>
      <div id="dashboardToast"
           class="toast-enter transition-all duration-300 mb-6 glass-card soft-shadow rounded-2xl border border-emerald-200/80 px-4 py-3">
        <p class="text-sm font-semibold text-emerald-700"><%= dashboardToast %></p>
      </div>
    <% } %>

    <!-- Featured Balance Section -->
    <div class="glass-card soft-shadow bg-gradient-to-br from-white/70 to-brand-purple-50/40 rounded-2xl border border-brand-purple-100/60 p-7 sm:p-10 md:p-12 mb-6 transition-all duration-200 relative overflow-hidden">
      <div class="absolute top-0 right-0 w-32 h-32 bg-brand-purple-100/20 rounded-full -mr-16 -mt-16"></div>
      <div class="absolute bottom-0 left-0 w-24 h-24 bg-brand-purple-50/30 rounded-full -ml-12 -mb-12"></div>
      <div class="text-center relative z-10">
        <p class="text-sm font-medium text-brand-purple mb-3 uppercase tracking-wide">Seu saldo atual</p>
        <h2 class="text-lg font-medium text-gray-600 mb-4">Saldo disponível</h2>
        <p class="text-4xl sm:text-5xl font-bold <%= balance >= 0 ? 'text-brand-purple' : 'text-rose-500' %>">
          <%= formatCurrency(balance) %>
        </p>
      </div>
    </div>

    <!-- Monthly Summary -->
    <div class="glass-card soft-shadow bg-gradient-to-r from-brand-purple-50/35 via-white/70 to-brand-purple-50/35 rounded-2xl p-4 mb-8 border border-brand-purple-100/60 mx-auto" style="max-width: 840px;">
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6">
        <div class="text-center">
          <p class="text-xs text-brand-purple mb-1 font-medium">Voce recebeu no periodo</p>
          <p class="text-lg font-medium text-emerald-600"><%= formatCurrency(totalIncome) %></p>
        </div>
        <div class="text-center sm:border-l sm:border-brand-purple-200/50 sm:pl-4 lg:pl-6">
          <p class="text-xs text-brand-purple mb-1 font-medium">Voce gastou no periodo</p>
          <p class="text-lg font-medium text-rose-600"><%= formatCurrency(totalExpenses) %></p>
        </div>
        <div class="text-center sm:col-span-2 lg:col-span-1 lg:border-l lg:border-brand-purple-200/50 lg:pl-6">
          <p class="text-xs text-brand-purple mb-1 font-medium">Despesas fixas (mensais)</p>
          <p class="text-lg font-medium text-brand-purple"><%= formatCurrency(totalFixedExpenses) %></p>
        </div>
      </div>
    </div>

    <!-- Monthly Projection -->
    <% const projection = monthlyProjection || {}; %>
    <div class="glass-card soft-shadow rounded-2xl border mb-8 overflow-hidden <%= projection.isPositive ? 'border-emerald-200/80' : 'border-rose-200/80' %>">
      <div class="px-6 py-4 <%= projection.isPositive ? 'bg-emerald-50/70' : 'bg-rose-50/70' %> border-b <%= projection.isPositive ? 'border-emerald-200/70' : 'border-rose-200/70' %>">
        <h2 class="text-lg font-bold <%= projection.isPositive ? 'text-emerald-700' : 'text-rose-700' %>">Previsao de gastos do mes</h2>
      </div>
      <div class="p-6">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-5">
          <div class="rounded-xl border border-gray-200 bg-white/70 p-4">
            <p class="text-xs uppercase tracking-wide text-gray-500 mb-1">Despesas fixas</p>
            <p class="text-lg font-bold text-gray-900"><%= formatCurrency(projection.fixedExpenses || 0) %></p>
          </div>
          <div class="rounded-xl border border-gray-200 bg-white/70 p-4">
            <p class="text-xs uppercase tracking-wide text-gray-500 mb-1">Media variavel (3m)</p>
            <p class="text-lg font-bold text-gray-900"><%= formatCurrency(projection.averageVariableExpenses || 0) %></p>
          </div>
          <div class="rounded-xl border border-gray-200 bg-white/70 p-4">
            <p class="text-xs uppercase tracking-wide text-gray-500 mb-1">Estimativa total do mes</p>
            <p class="text-lg font-bold text-gray-900"><%= formatCurrency(projection.estimatedTotalMonthExpense || 0) %></p>
          </div>
        </div>
        <div class="rounded-xl px-4 py-4 <%= projection.isPositive ? 'bg-emerald-100/70' : 'bg-rose-100/70' %>">
          <p class="text-xs uppercase tracking-wide mb-1 <%= projection.isPositive ? 'text-emerald-700' : 'text-rose-700' %>">Saldo projetado</p>
          <p class="text-3xl font-bold <%= projection.isPositive ? 'text-emerald-700' : 'text-rose-700' %>"><%= formatCurrency(projection.projectedBalance || 0) %></p>
        </div>
      </div>
    </div>


    <!-- Credit Card Financial Summary -->
    <% const summary = (typeof creditCardSummary !== 'undefined' && creditCardSummary) ? creditCardSummary : {}; %>
    <div class="mb-8">
      <div class="glass-card soft-shadow rounded-2xl overflow-hidden <%= summary.isRisk ? 'border-rose-300' : 'border-brand-purple-100' %>">
        <div class="px-6 py-4 bg-gradient-to-r <%= summary.isRisk ? 'from-rose-50 via-white to-rose-50' : 'from-brand-purple-50/60 via-white to-brand-purple-50/60' %>">
          <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
            <h2 class="text-xl font-bold <%= summary.isRisk ? 'text-rose-700' : 'text-gray-900' %>">Resumo do Cartao</h2>
            <a href="/credit-cards" class="text-sm font-medium <%= summary.isRisk ? 'text-rose-600 hover:text-rose-700' : 'text-brand-purple hover:text-brand-purple-dark' %>">
              Gerenciar cartoes
            </a>
          </div>
        </div>

        <% if (!summary.hasCards) { %>
          <div class="px-6 py-8 text-center">
            <p class="text-gray-700 mb-4">Voce ainda nao possui cartao cadastrado.</p>
            <a href="/credit-cards/add" class="inline-block bg-brand-purple hover:bg-brand-purple-dark text-white font-medium py-2 px-5 rounded-lg transition-colors">
              + Adicionar Cartao
            </a>
          </div>
        <% } else { %>
          <div class="p-6">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
              <div class="rounded-xl border <%= summary.isRisk ? 'border-rose-200 bg-rose-50/50' : 'border-brand-purple-100 bg-brand-purple-50/30' %> p-4">
                <p class="text-xs uppercase tracking-wide mb-1 <%= summary.isRisk ? 'text-rose-600' : 'text-brand-purple' %>">Fatura Atual</p>
                <p class="text-2xl font-bold <%= summary.isRisk ? 'text-rose-700' : 'text-gray-900' %>"><%= formatCurrency(summary.currentInvoice) %></p>
              </div>

              <div class="rounded-xl border border-gray-200 bg-gray-50 p-4">
                <p class="text-xs uppercase tracking-wide mb-1 text-gray-500">Fechamento</p>
                <p class="text-xl font-bold text-gray-900">
                  <%= summary.nextClosingDate ? formatDate(summary.nextClosingDate) : '-' %>
                </p>
              </div>

              <div class="rounded-xl border border-gray-200 bg-gray-50 p-4">
                <p class="text-xs uppercase tracking-wide mb-1 text-gray-500">Vencimento</p>
                <p class="text-xl font-bold text-gray-900">
                  <%= summary.nextDueDate ? formatDate(summary.nextDueDate) : '-' %>
                </p>
              </div>
            </div>

            <div class="rounded-xl border <%= summary.isRisk ? 'border-rose-200 bg-rose-50/40' : 'border-brand-purple-100 bg-brand-purple-50/20' %> p-4">
              <div class="flex items-center justify-between mb-2">
                <p class="text-sm font-medium <%= summary.isRisk ? 'text-rose-700' : 'text-gray-700' %>">Limite Utilizado</p>
                <p class="text-sm font-bold <%= summary.isRisk ? 'text-rose-700' : 'text-brand-purple' %>"><%= Math.round(summary.usedPercent || 0) %>%</p>
              </div>

              <div class="w-full h-3 rounded-full bg-gray-200 overflow-hidden">
                <div
                  class="h-full rounded-full transition-all duration-500 <%= summary.isRisk ? 'bg-gradient-to-r from-rose-500 to-red-600' : 'bg-gradient-to-r from-brand-purple to-brand-purple-light' %>"
                  style="width: <%= Math.max(0, Math.min(100, summary.progressPercent || 0)) %>%;">
                </div>
              </div>

              <div class="flex items-center justify-between mt-3 text-xs">
                <span class="<%= summary.isRisk ? 'text-rose-600' : 'text-gray-500' %>">
                  Limite total: <strong><%= formatCurrency(summary.totalLimit) %></strong>
                </span>
                <% if (summary.isRisk) { %>
                  <span class="font-semibold text-rose-700">Risco de estouro</span>
                <% } else { %>
                  <span class="font-semibold text-emerald-600">Dentro do planejado</span>
                <% } %>
              </div>
            </div>
          </div>
        <% } %>
      </div>
    </div>

    <!-- Transactions Section -->
    <div class="mb-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-6 flex items-center">
        <span class="w-1 h-8 bg-brand-purple rounded-full mr-3"></span>
        Transacoes
      </h2>

      <% const safeTransactionGroups = Array.isArray(transactionGroups) ? transactionGroups : []; %>

      <% if (safeTransactionGroups.length === 0) { %>
        <div class="glass-card soft-shadow text-center py-16 bg-gradient-to-br from-brand-purple-50/20 to-white/70 rounded-2xl border border-brand-purple-100/50">
          <h3 class="text-lg font-medium text-gray-900 mb-2">Nenhuma transacao</h3>
          <p class="text-brand-purple">Adicione sua primeira transacao.</p>
        </div>
      <% } else { %>
        <% safeTransactionGroups.forEach((group, groupIndex) => { %>
          <div class="<%= groupIndex < safeTransactionGroups.length - 1 ? 'mb-6' : '' %>">
            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 mb-2 px-4 py-3 rounded-xl bg-gradient-to-r from-brand-purple-50/50 via-white to-brand-purple-50/20 border border-brand-purple-100/70">
              <h3 class="text-sm sm:text-base font-semibold text-gray-900 tracking-wide">
                <%= group && group.dateLabel ? group.dateLabel : 'Data nao definida' %>
              </h3>
              <div class="flex items-center gap-2 text-xs">
                <% if (group && group.totalIncome && group.totalIncome > 0) { %>
                  <span class="px-2.5 py-1 rounded-full bg-emerald-50 text-emerald-700 font-semibold">
                    + <%= formatCurrency(group.totalIncome) %>
                  </span>
                <% } %>
                <% if (group && group.totalExpenses && group.totalExpenses > 0) { %>
                  <span class="px-2.5 py-1 rounded-full bg-rose-50 text-rose-700 font-semibold">
                    - <%= formatCurrency(group.totalExpenses) %>
                  </span>
                <% } %>
              </div>
            </div>

            <div class="glass-card soft-shadow rounded-2xl border border-gray-100 overflow-hidden">
              <% if (group && group.transactions && Array.isArray(group.transactions) && group.transactions.length > 0) { %>
                <% group.transactions.forEach((transaction, index) => { %>
                  <div data-transaction-id="<%= transaction && transaction.id ? transaction.id : '' %>"
                       class="group px-4 sm:px-5 py-4 sm:py-5 transition-all duration-300 hover:bg-brand-purple-50/20 <%= index < group.transactions.length - 1 ? 'border-b border-gray-100' : '' %>">
                    <div class="flex flex-col sm:flex-row sm:items-center gap-3 sm:gap-4">
                      <div class="flex-shrink-0 w-10 h-10 sm:w-11 sm:h-11 rounded-full flex items-center justify-center <%= transaction && transaction.type === 'income' ? 'bg-emerald-50 text-emerald-600' : 'bg-rose-50 text-rose-600' %>">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" viewBox="0 0 20 20" fill="currentColor">
                          <% if (transaction && transaction.type === 'income') { %>
                            <path fill-rule="evenodd" d="M10 3a1 1 0 011 1v7.586l2.293-2.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 111.414-1.414L9 11.586V4a1 1 0 011-1z" clip-rule="evenodd" />
                          <% } else { %>
                            <path fill-rule="evenodd" d="M10 17a1 1 0 01-1-1V8.414L6.707 10.707a1 1 0 11-1.414-1.414l4-4a1 1 0 011.414 0l4 4a1 1 0 11-1.414 1.414L11 8.414V16a1 1 0 01-1 1z" clip-rule="evenodd" />
                          <% } %>
                        </svg>
                      </div>

                      <div class="flex-1 min-w-0 order-1 sm:order-none">
                        <div class="flex items-start justify-between gap-3 sm:gap-2">
                          <p class="text-sm sm:text-base font-semibold text-gray-900 leading-snug">
                            <%= transaction && transaction.description ? transaction.description : 'Sem descricao' %>
                          </p>
                          <p class="text-base sm:text-xl font-bold whitespace-nowrap <%= transaction && transaction.type === 'income' ? 'text-emerald-600' : 'text-rose-600' %>">
                            <%= transaction && transaction.type === 'income' ? '+' : '-' %><%= formatCurrency(Math.abs(transaction && transaction.amount ? transaction.amount : 0)) %>
                          </p>
                        </div>
                        <div class="flex flex-wrap items-center gap-2 mt-2">
                          <span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-[11px] font-semibold <%= transaction && transaction.type === 'income' ? 'bg-emerald-100 text-emerald-700' : 'bg-rose-100 text-rose-700' %>">
                            <%= transaction && transaction.type === 'income' ? 'Entrada' : 'Saida' %>
                          </span>
                          <span class="inline-flex items-center gap-1 rounded-full px-2.5 py-0.5 text-[11px] font-semibold text-gray-700 bg-white border border-gray-200">
                            <span class="w-2 h-2 rounded-full" style="background-color: <%= transaction && transaction.category_color ? transaction.category_color : '#8A05BE' %>;"></span>
                            <%= transaction && transaction.category_name ? transaction.category_name : 'Sem categoria' %>
                          </span>
                          <span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-[11px] font-semibold border <%= transaction && transaction.payment_method === 'credit' ? 'bg-violet-50 text-violet-700 border-violet-200' : transaction && transaction.payment_method === 'debit' ? 'bg-sky-50 text-sky-700 border-sky-200' : 'bg-amber-50 text-amber-700 border-amber-200' %>">
                            <%= transaction && transaction.payment_method === 'credit' ? 'Credito' : transaction && transaction.payment_method === 'debit' ? 'Debito' : 'Dinheiro' %>
                          </span>
                          <% if (transaction && Number(transaction.is_recurring || 0) === 1) { %>
                            <span class="inline-flex items-center rounded-full px-2.5 py-0.5 text-[11px] font-semibold bg-brand-purple-50 text-brand-purple border border-brand-purple-200">
                              Fixo
                            </span>
                          <% } %>
                        </div>
                        <p class="text-xs text-gray-400 mt-2">
                          <%= transaction && transaction.date ? formatDate(transaction.date) : '' %>
                        </p>
                      </div>

                      <div class="order-2 sm:order-none">
                        <div class="mt-1 flex items-center justify-end sm:justify-start gap-2 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity">
                          <a href="/transactions/edit/<%= transaction && transaction.id ? transaction.id : '' %>"
                             class="text-xs px-3 py-2 min-h-[44px] rounded-md bg-gray-100 hover:bg-brand-purple-100 text-gray-700 hover:text-brand-purple inline-flex items-center">
                            Editar
                          </a>
                          <form action="/transactions/delete/<%= transaction && transaction.id ? transaction.id : '' %>" method="POST" class="inline"
                                onsubmit="return confirm('Tem certeza que deseja excluir esta transacao?')">
                            <button type="submit"
                                    class="text-xs px-3 py-2 min-h-[44px] rounded-md bg-rose-50 hover:bg-rose-100 text-rose-700 inline-flex items-center">
                              Excluir
                            </button>
                          </form>
                        </div>
                      </div>
                    </div>
                  </div>
                <% }); %>
              <% } %>
            </div>
          </div>
        <% }); %>
      <% } %>
    </div>

    <!-- Floating Action Menu -->
    <div class="fixed bottom-5 right-4 sm:bottom-6 sm:right-6 z-50 flex flex-col items-end gap-3">
      <div id="actionMenu"
           class="origin-bottom-right opacity-0 scale-95 pointer-events-none transition-all duration-200 ease-out flex flex-col gap-2">
        <a href="/transactions/add?type=income"
           class="glass-card min-h-[44px] border border-brand-purple-100 text-brand-purple soft-shadow rounded-2xl px-4 py-2 text-sm font-semibold hover:bg-brand-purple-50/70 transition-colors inline-flex items-center">
          Nova receita
        </a>
        <a href="/transactions/add?type=expense"
           class="glass-card min-h-[44px] border border-brand-purple-100 text-brand-purple soft-shadow rounded-2xl px-4 py-2 text-sm font-semibold hover:bg-brand-purple-50/70 transition-colors inline-flex items-center">
          Nova despesa
        </a>
        <button id="openCardPurchaseModal"
           type="button"
           class="glass-card min-h-[44px] border border-brand-purple-100 text-brand-purple soft-shadow rounded-2xl px-4 py-2 text-sm font-semibold hover:bg-brand-purple-50/70 transition-colors text-left">
          Nova compra no cartao
        </button>
      </div>

      <button id="actionFab"
              type="button"
              aria-label="Abrir menu de acoes"
              class="w-14 h-14 sm:w-16 sm:h-16 min-h-[44px] bg-gradient-to-br from-brand-purple to-brand-purple-dark hover:from-brand-purple-dark hover:to-brand-purple rounded-full shadow-2xl flex items-center justify-center text-white text-3xl font-bold transition-all duration-300 hover:scale-110">
        +
      </button>
    </div>

    <% const safeDashboardCards = Array.isArray(dashboardCards) ? dashboardCards : []; %>
    <div id="cardPurchaseModal" class="fixed inset-0 z-[60] hidden items-center justify-center p-4">
      <div id="cardPurchaseBackdrop" class="absolute inset-0 bg-black/30 opacity-0 transition-opacity duration-200"></div>
      <div id="cardPurchasePanel" class="glass-card soft-shadow relative w-full max-w-md rounded-2xl border border-brand-purple-100 p-6 opacity-0 scale-95 transition-all duration-200">
        <div class="flex items-center justify-between mb-4">
          <h3 class="text-lg font-bold text-gray-900">Nova compra no cartao</h3>
          <button id="closeCardPurchaseModal" type="button" class="text-gray-500 hover:text-gray-700 text-xl leading-none">&times;</button>
        </div>
        <% if (safeDashboardCards.length === 0) { %>
          <p class="text-sm text-gray-600 mb-4">Voce ainda nao tem cartoes cadastrados.</p>
          <a href="/credit-cards/add" class="inline-block bg-brand-purple hover:bg-brand-purple-dark text-white font-medium py-2 px-4 rounded-lg transition-colors">
            Adicionar cartao
          </a>
        <% } else { %>
          <div class="flex flex-col gap-2 max-h-64 overflow-auto pr-1">
            <% safeDashboardCards.forEach((card) => { %>
              <a href="/credit-cards/<%= card.id %>/add-transaction"
                 class="w-full rounded-xl border border-brand-purple-100 bg-brand-purple-50/30 hover:bg-brand-purple-50 text-gray-800 px-4 py-3 text-sm font-semibold transition-colors">
                <%= card.name %>
              </a>
            <% }); %>
          </div>
        <% } %>
      </div>
    </div>

    <script>
      // Mostrar mês atual no dashboard
      document.addEventListener('DOMContentLoaded', function() {
        const currentMonthElement = document.getElementById('currentMonth');
        const selectedMonthLabel = <%- JSON.stringify(typeof selectedMonthLabel !== 'undefined' ? selectedMonthLabel : '') %>;
        if (currentMonthElement) {
          currentMonthElement.textContent = selectedMonthLabel || '';
        }

        const monthFilter = document.getElementById('monthFilter');
        const monthFilterForm = document.getElementById('monthFilterForm');
        if (monthFilter && monthFilterForm) {
          monthFilter.addEventListener('change', function() {
            monthFilterForm.submit();
          });
        }

        const toastEl = document.getElementById('dashboardToast');
        const highlightedTxId = <%- JSON.stringify(typeof highlightedTransactionId !== 'undefined' ? highlightedTransactionId : null) %>;

        if (toastEl) {
          requestAnimationFrame(function() {
            toastEl.classList.add('toast-show');
          });

          setTimeout(function() {
            toastEl.classList.remove('toast-show');
            toastEl.classList.add('toast-enter');
          }, 2600);

          setTimeout(function() {
            if (toastEl && toastEl.parentNode) {
              toastEl.parentNode.removeChild(toastEl);
            }
          }, 3000);
        }

        if (highlightedTxId) {
          const txEl = document.querySelector(`[data-transaction-id=\"${highlightedTxId}\"]`);
          if (txEl) {
            txEl.classList.add('tx-highlight');
            txEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
          }
        }

        if (window.history && window.history.replaceState) {
          const cleanUrl = new URL(window.location.href);
          cleanUrl.searchParams.delete('toast');
          cleanUrl.searchParams.delete('tx');
          window.history.replaceState({}, '', cleanUrl.pathname + cleanUrl.search + cleanUrl.hash);
        }

        const actionFab = document.getElementById('actionFab');
        const actionMenu = document.getElementById('actionMenu');
        const openCardPurchaseModalBtn = document.getElementById('openCardPurchaseModal');
        const cardPurchaseModal = document.getElementById('cardPurchaseModal');
        const cardPurchaseBackdrop = document.getElementById('cardPurchaseBackdrop');
        const cardPurchasePanel = document.getElementById('cardPurchasePanel');
        const closeCardPurchaseModalBtn = document.getElementById('closeCardPurchaseModal');
        let isActionMenuOpen = false;
        let isCardPurchaseModalOpen = false;

        function setActionMenuState(open) {
          isActionMenuOpen = open;
          if (!actionFab || !actionMenu) return;

          if (open) {
            actionMenu.classList.remove('opacity-0', 'scale-95', 'pointer-events-none');
            actionMenu.classList.add('opacity-100', 'scale-100');
            actionFab.classList.add('rotate-45');
          } else {
            actionMenu.classList.add('opacity-0', 'scale-95', 'pointer-events-none');
            actionMenu.classList.remove('opacity-100', 'scale-100');
            actionFab.classList.remove('rotate-45');
          }
        }

        function setCardPurchaseModalState(open) {
          isCardPurchaseModalOpen = open;
          if (!cardPurchaseModal || !cardPurchaseBackdrop || !cardPurchasePanel) return;

          if (open) {
            cardPurchaseModal.classList.remove('hidden');
            cardPurchaseModal.classList.add('flex');
            requestAnimationFrame(function() {
              cardPurchaseBackdrop.classList.remove('opacity-0');
              cardPurchaseBackdrop.classList.add('opacity-100');
              cardPurchasePanel.classList.remove('opacity-0', 'scale-95');
              cardPurchasePanel.classList.add('opacity-100', 'scale-100');
            });
          } else {
            cardPurchaseBackdrop.classList.add('opacity-0');
            cardPurchaseBackdrop.classList.remove('opacity-100');
            cardPurchasePanel.classList.add('opacity-0', 'scale-95');
            cardPurchasePanel.classList.remove('opacity-100', 'scale-100');
            setTimeout(function() {
              if (!isCardPurchaseModalOpen) {
                cardPurchaseModal.classList.add('hidden');
                cardPurchaseModal.classList.remove('flex');
              }
            }, 200);
          }
        }

        if (actionFab && actionMenu) {
          actionFab.addEventListener('click', function() {
            setActionMenuState(!isActionMenuOpen);
          });

          if (openCardPurchaseModalBtn) {
            openCardPurchaseModalBtn.addEventListener('click', function() {
              setActionMenuState(false);
              setCardPurchaseModalState(true);
            });
          }

          if (closeCardPurchaseModalBtn) {
            closeCardPurchaseModalBtn.addEventListener('click', function() {
              setCardPurchaseModalState(false);
            });
          }

          if (cardPurchaseBackdrop) {
            cardPurchaseBackdrop.addEventListener('click', function() {
              setCardPurchaseModalState(false);
            });
          }

          document.addEventListener('click', function(event) {
            const clickedInsideMenu = actionMenu.contains(event.target);
            const clickedFab = actionFab.contains(event.target);
            if (!clickedInsideMenu && !clickedFab) {
              setActionMenuState(false);
            }
          });

          document.addEventListener('keydown', function(event) {
            if (event.key === 'Escape') {
              setActionMenuState(false);
              setCardPurchaseModalState(false);
            }
          });
        }
      });
    </script>
  </div></body>
</html>













